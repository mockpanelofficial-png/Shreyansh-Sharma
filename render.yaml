services:
  - type: web
    name: shreyansh-sharma-portfolio
    runtime: node
    rootDir: Shreyansh-Sharma
    buildCommand: npm install && npm run build
    startCommand: npm start
    healthCheckPath: /api/health